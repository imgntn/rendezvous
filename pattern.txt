//the client sends the server an initial message with a device id 

//if no record
//server stores a record



deviceID:String,
	dateCreated: Date,
	uuid: String,
	regCode:String,
	regCodeExpiration: Date,
	authorized:String,
	authorizationExpiration:String,
	message:String,

//server sends the regcode back to the client

//the client displays the regcode


user sends user / pass / regcode to provider 
provider verifies user account 

provider sends message to server with device id and expiration a token(if necessary)

//server associates device id 


//client polls server 

//to unlink an account, delete the record for that device id